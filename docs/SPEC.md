# Technical spec — <product name>

1. Architecture diagram (Next.js → Go API → Postgres; Go → LLM)
2. Data model (tables, key fields, relations)
3. API contract — every endpoint: method, path, request JSON, response JSON, errors
4. AI design — agents/steps, prompts, JSON schemas of structured outputs, tools, fallback/mock behaviour
5. Frontend — routes, screens, components (shadcn/ObsidianUI), states
6. Seed / demo data
7. Deploy — Railway services, env vars, build commands
8. Task split by person and by hour
